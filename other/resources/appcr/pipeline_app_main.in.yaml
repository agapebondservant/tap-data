apiVersion: kappctrl.k14s.io/v1alpha1
kind: App
metadata:
  name: ml-image-processing-pipeline-app-a
spec:
  serviceAccountName: default
  fetch:
    - git:
        url: https://github.com/${DATA_E2E_GIT_USER}/sample-ml-app
        ref: origin/main-${DOLLAR}${SESSION_NAMESPACE}

  template:
    - ytt:
        paths:
          - pipeline.yaml
          - values.yaml

  deploy:
    - kapp: {}